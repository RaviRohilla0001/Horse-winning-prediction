# Horse-winning-prediction
This program is a complete machine learning pipeline for predicting horse race performance using historical race data. It automatically reads multiple CSV files containing horse race records, cleans and preprocesses the data, and combines them into a single dataset for modeling. 
